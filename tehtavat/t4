def suurin_luku() -> int:
    luvut = []
    
    with open("luvut.txt") as tiedosto:
        for rivi in tiedosto:
            luvut = rivi.strip().split(",")
            for luku in luvut:
                luvut.append(luku)
                
                return max(luvut)